import { TradingSortPipe } from './trading-sort.pipe';

describe('TradingSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TradingSortPipe();
    expect(pipe).toBeTruthy();
  });
});
