import { CurrencySortPipe } from './currency-sort.pipe';

describe('CurrencySortPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencySortPipe();
    expect(pipe).toBeTruthy();
  });
});
